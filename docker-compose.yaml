services:
  db:
    image: postgres:16-alpine
    container_name: silentthreads-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: silentthreads_dev
    ports:
      - "5432:5432"
